<h3>注文情報入力</h3>
<%= form_with model: @order, url: orders_confirm_path, method: :get do |f|  %>
<div class="container mt-3">
    <h3>支払方法</h3>
    <div>
      <%= f.radio_button :payment_method, 0, checked: true %>
      <%= f.label :payment_method, "クレジットカード" %>
    </div>
    <div>
      <%= f.radio_button :payment_method, 1 %>
      <%= f.label :payment_method, "銀行振込" %>
    </div>

    <h3>お届け先</h3>
    <div>
      <%= f.radio_button :address, [current_customer.postal_code, "　", current_customer.address, "　", current_customer.last_name, current_customer.first_name ], checked: true %>
      <%= f.label :address, "ご自身の住所" %>
    </div>
    <div>
      <%= f.radio_button :address, :registered_address %>
      <%= f.label :address, "登録済住所から選択" %>
      <%= f.select :address, @addresses.map { |a| [a.name] } %>
      <%= hidden_field_tag "order[address_id]" %>
    </div>
  </div>


  <h3>お届け先</h3>

  <!--, Address.where(customer_id: current_customer.id).pluck(:id).first-->
  <div>
    <%= f.radio_button :address, current_customer.address, checked: true %>
    <%= f.label :address, "ご自身の住所" %>
  </div>
  <div>
    〒<%= current_customer.postal_code + "　" + current_customer.address %>
  </div>
  <div><%= current_customer.last_name + current_customer.first_name %></div>
  <div>
    <!--%= f.radio_button :address,  %>-->
    <%= f.label :address, "登録済住所から選択" %>
    <!--%= f.collection_select :address, Address.current_customer.all, :address, 〒current_customer.postal_code + "　" + current_customer.address + "" + current_customer.last_name + current_customer.first_name %>-->
  </div>


<% end %>

以下テスト
<div><%= current_customer.first_name_kana %></div>
