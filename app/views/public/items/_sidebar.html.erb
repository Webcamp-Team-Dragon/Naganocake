<div class="col-md-3">
  <table class="table table-hover table-bordered">
    <thead>
      <tr>
        <th class="active-text-center"><h5>ジャンル検索</h5></th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
      <tr>
        <td><%=link_to genre.name, genre_path(genre.id) %></td>
      </tr>
      <tr>
        
      </tr>
      <% end %>
    </tbody>
  </table>
</div>