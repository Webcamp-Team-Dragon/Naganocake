<div class="container">
  <div class="row">
    <%= render 'public/items/sidebar', genres: @genres %>
    <div class="col-xs-9">
      <h2>商品一覧</h2><h5>(全<%= @total_items %>件)</h5>
        <div class="d-flex flex-wrap">
          <% @items.each do |item| %>
          <!--画像が入ったらコメントアウトを外してください！-->
            <!--%= link_to public_item_path(item.id) do %>-->
            <!--  %= attachment_image_tag item, :image, size: "120×150" %>-->
            <!--% end %>-->
          <!--ここまで-->
            <p><%= item.name %></p>
            <p>¥<%= item.price %></p>
          <% end %>
        </div>
    </div>
  </div>
</div>
